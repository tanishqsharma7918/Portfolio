{
  "name": "portfolio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Portfolio-upgrade && npm install && npm run build",
    "vercel-build": "cd Portfolio-upgrade && npm install && npm run build"
  }
}
